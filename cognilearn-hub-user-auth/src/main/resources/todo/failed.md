QPS表示服务器每秒能响应的查询请求数量，用于量化系统的吞吐能力，公式为：
**QPS = 总请求量 / 时间周期（秒）**



验证码的发送流程与验证流程

最开始只有redis，为了高可用，redis不可用时，依旧可以使用，redis作为加速，mysql兜底，写时双写，读时读redis，不成功或读不到读mysql（失败的，过度工程化，不做了）


5.注册流程用redis的分布式锁做用户名的加锁处理： redis不可用降级，直接让请求通过，数据库中(user_name, is_del（默认0）, del_version（默认0）)唯一索引兜底 （失败的，过度工程化，已经做了）
