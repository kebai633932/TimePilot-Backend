<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.cxk.infrastructure.adapter.dao.IUserDao">

    <resultMap id="userMap" type="org.cxk.infrastructure.adapter.dao.po.User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="real_name" property="realName"/>
        <result column="password" property="password"/>
        <result column="email" property="email"/>
        <result column="phone" property="phone"/>
        <result column="is_deleted" property="isDeleted"/>
        <result column="last_login_time" property="lastLoginTime"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <insert id="insert" parameterType="org.cxk.infrastructure.adapter.dao.po.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (id, username, real_name, password, email, phone, is_deleted, last_login_time, create_time, update_time)
        VALUES (#{id}, #{username}, #{realName}, #{password}, #{email}, #{phone}, #{isDeleted}, #{lastLoginTime}, #{createTime}, #{updateTime})
    </insert>

    <select id="findByUsername" parameterType="java.lang.String" resultMap="userMap">
        SELECT * FROM user WHERE username = #{username} AND is_deleted = false
    </select>

    <select id="findAll" resultMap="userMap">
        SELECT * FROM user WHERE is_deleted = false
    </select>

</mapper>
